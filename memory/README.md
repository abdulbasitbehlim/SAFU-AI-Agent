# Personal memory

SAFU's real `long_term.json` and any memory databases are local personal data and
are intentionally ignored by Git.

`long_term.example.json` is an empty schema example only. Do not replace it with
your own memory file in a public repository.
